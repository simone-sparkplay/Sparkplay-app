## Packages
canvas-confetti | For gamified celebration when completing a mission
@types/canvas-confetti | Types for canvas-confetti

## Notes
- `api` object from `@shared/routes` is used for all API calls and runtime validation.
- The app uses a highly playful, gamified, mobile-first design system with bouncy animations using `framer-motion`.
- Bottom navigation is used for core mobile navigation (Home, Missions, Diary).
